from python:3.11-alpine3.19

COPY ./devpanel /devpanel

WORKDIR /devpanel

RUN pip install -r requirements.txt

RUN adduser -D -s /bin/ash dev

RUN chown -R dev:dev /devpanel

USER dev

CMD ["python", "-B", "app.py"]